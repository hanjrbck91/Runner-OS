# tests — cross-cutting integration & E2E (M07-C+)

Placeholder. Repository integration tests against a real disposable PostgreSQL
(Neon branch / Docker) arrive in M07-C; API tests in M07-D; Playwright E2E in
M07-E+. Pure domain/application unit tests live beside the code they cover in
`packages/*/tests`.
