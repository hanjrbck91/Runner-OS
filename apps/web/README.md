# apps/web — Next.js PWA + API (M07-D / M07-E)

Placeholder. The Next.js App Router web client and Route Handler API land in
M07-D (API) and M07-E (PWA UI). Both consume `@runner-os/core` services through
the ports; the UI never touches Postgres, Drive, or Sheets, and holds no secrets.
Nothing here yet — do not build until those milestones are approved.
